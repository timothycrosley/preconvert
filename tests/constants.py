"""Provides constants that can be used across all test cases"""
import os

TEST_DIRECTORY = os.path.dirname(os.path.realpath(__file__))
BASE_DIRECTORY = os.path.realpath(os.path.join(TEST_DIRECTORY, ".."))
